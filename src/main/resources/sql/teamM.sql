create table teamm_subscriptions (
                                     name varchar(100) not null,
                                     price numeric not null,
                                     due_date date not null
);